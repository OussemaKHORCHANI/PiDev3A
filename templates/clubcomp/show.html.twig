{% extends 'base2.html.twig' %}

{% block title %}Clubcomp{% endblock %}

{% block body %}
    <h1>Club participer à {{ competition.nomCompetition }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom Club</th>
            </tr>
            {% for clubco in clubcomp %}
                {% if competition.nomCompetition is same as clubco.idCompetition.nomCompetition %}
            <tr>
                <td>{{ clubco.idClub.nomClub }}</td>
            </tr>
                {% endif %}
            {% else %}
                <tr>
                    <td colspan="7">no records found</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('competition_index') }}">Back to list / </a>


    {{ include('clubcomp/_delete_form.html.twig') }}
{% endblock %}
