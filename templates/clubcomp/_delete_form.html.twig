<form method="post" action="{{ path('clubcomp_delete', {'idclubcomp': clubcomp.idclubcomp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clubcomp.idclubcomp) }}">
    <button class="btn">Delete</button>
</form>
