{% extends 'base2.html.twig' %}

{% block title %}Clubcomp index{% endblock %}

{% block body %}
    <h1>Compétition</h1>
<div class="row">
    <div class="col-lg-12">
        <section class="panel">
            <header class="panel-heading">
                <div>
                    <!--  search form start -->


                    <form class="navbar-form">
                        <input class="form-control" placeholder="Rechercher" type="text">
                        <button class="btn btn-success my-2 my-sm-0" type="submit">Rechercher</button>
                    </form>


                    <!--  search form end -->
                </div>
            </header>
    <table class="table table-striped table-advance table-hover">
        <thead>
            <tr>
                <th>NomCompetition</th>
                <th>DateDebut</th>
                <th>DateFin</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for clubcomp in clubcomps %}
            <tr>
                <td>{{ clubcomp.idCompetition.nomCompetition }}</td>
                <td>{{ clubcomp.idCompetition.dateDebut ? clubcomp.idCompetition.dateDebut|date('Y-m-d') : '' }}</td>
                <td>{{ clubcomp.idCompetition.dateFin ? clubcomp.idCompetition.dateFin|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('clubcomp_show', {'idclubcomp': clubcomp.idclubcomp}) }}">Show / </a>
                    <a href="{{ path('clubcomp_edit', {'idclubcomp': clubcomp.idclubcomp}) }}">Edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
        </section>
    </div>
</div>
    <a href="{{ path('clubcomp_new') }}">Create new</a>
{% endblock %}
