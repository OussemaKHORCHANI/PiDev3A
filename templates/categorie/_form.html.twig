{{ form_start(form, {attr: {'novalidate-off': 'novalidate'}})}}
    {{ form_widget(form) }}
<center>
    <button class="btn">{{ button_label|default('Save') }}</button>
</center>
{{ form_end(form) }}
