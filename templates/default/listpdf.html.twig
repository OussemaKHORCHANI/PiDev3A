
    <h1 ALIGN="center">Liste des produits</h1><br>
<table border="2" >

    <thead>
    <tr>
        <th>imageArticle</th>
        <th>ref</th>
        <th>libelle</th>
        <th>Catégorie</th>
        <th>qtarticle</th>
        <th>prix</th>
        <th>Rate</th>

    </tr>
    </thead>
    <tbody>
   {% for c in listpdf %}
   <tr>
        <td>{{ c.imageArticle}} </td>
        <td>{{ c.ref}}</td>
        <td>{{ c.libelle }}</td>
        <td>{{ c.idcat}} </td>
        <td>{{ c.qtarticle}} </td>
        <td>{{ c.prix }}</td>
        <td>{{ c.rate }}</td>

    </tr >
   {% endfor %}
    </tbody>
</table>
