{% extends 'base2.html.twig' %}

{% block title %}Equipe index{% endblock %}

{% block head %}
<section id="main-content">
    <section class="wrapper">
        <!--overview start-->
        <div class="row">
            <div class="col-lg-12">

                <ol class="breadcrumb">
                    <li><i class="fa fa-home"></i><a href="index.html">Home</a></li>
                    <li><i class="fa fa-bank"></i>Equipes</li>
                </ol>
            </div>
        </div>
        {% endblock %}
        {% block formullist %}
    <h1>Gérer Equipes </h1>
    

  <div class="row">
    <div class="col-lg-12">
        <section class="panel">
            <header class="panel-heading">

                <form class="navbar-form">
                <input class="form-control" id="myInput" type="text" id="navbar-search-input" placeholder="Search now" aria-label="search" aria-describedby="search">
                    <a  class="btn btn-primary" href="{{ path('equipe_imprimer') }}">Imprimer PDF</a>
                </form>
            </header>

            <table class="table table-striped table-advance table-hover">
                <tbody id ="hi">
                <tr>
                    <th><i class="icon_profile" ></i> Id</th>
                    <th><i class="icon_calendar"></i> nom</th>
                    <th><i class="icon_mail_alt"></i> Nombre</th>
                    
                    
                    <th><i class="icon_cogs"></i> Action</th>
                </tr>
        
        {% for equipe in equipes %}
            <tr>
                <td>{{ equipe.idequipe }}</td>
                <td>{{ equipe.nom }}</td>
                <td>{{ equipe.nombre }}</td>
                <td>
                    <a href="{{ path('equipe_show', {'idequipe': equipe.idequipe}) }}" class=" btn btn-primary">show</a>
                    <a href="{{ path('equipe_edit', {'idequipe': equipe.idequipe}) }}" class="btn btn-success">edit</a>
                </td>
            </tr>
                    </td>
                </tr>



        {% else %}
            <tr>
                <td colspan="7">Aucune Equipe</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<input class="form-control" id="myInput" type="text" id="navbar-search-input" placeholder="Search now" aria-label="search" aria-describedby="search">
        </section></div></div></section></section>
        </section>
    </div>
</div>
        
        
   

    <a href="{{ path('equipe_new') }}" class=" btn btn-primary">Ajouter</a>
{% endblock %}
