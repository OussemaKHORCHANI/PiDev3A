
{#{{  form_start(form,{'attr':{'novalidate': 'novalidate','class':'form-quick-post',id : "feedback_form"}}) }}
<table ALIGN="Center" >
    {{ form_errors(form.nomterrain) }}
    {{ form_label(form.nomterrain,"Nom du terrain") }}
    {{ form_widget(form.nomterrain) }}
    {{ form_errors(form.adresse) }}
    {{ form_label(form.adresse,"Adresse") }}
    {{ form_widget(form.adresse)}}
    {{ form_errors(form.etat) }}
    {{ form_label(form.etat,"Etat") }}
    {{ form_widget(form.etat)}}
    {{ form_errors(form.description) }}
    {{ form_label(form.description,"Description") }}
    {{ form_widget(form.description)}}
    {{ form_errors(form.photo) }}
    {{ form_label(form.photo,"photo") }}
    {{ form_widget(form.photo)}}
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}  #}

    <div class="row" align="center">
        <div class="col-lg-12">
            <section class="panel" align="center">
                <header class="panel-heading">

                </header>
                <div class="panel-body">
                    <div class="form" align="center">
                        {{  form_start(form,{'attr':{'novalidate': 'novalidate'}}) }}
                            <div class="form-group " align="center">
                                <div class="col-lg-10">
                                    {{ form_row(form.nomterrain,{'label':'Nom du terrain'} ) }}
                                </div>
                            </div>
                            <div class="form-group ">
                                <div class="col-lg-10">
                                    {{ form_row(form.adresse,{'label':'Adresse'} ) }}
                                </div>
                            </div>
                            <div class="form-group ">
                                <div class="col-lg-10">
                                    {{ form_row(form.etat,{'label':'Etat','attr': {'class': ' form-select-lg'} }) }}
                                </div> 
                            </div>
                            <div class="form-group ">
                                <div class="col-lg-10">
                                    {{ form_row(form.description,{'label':'Description'} ) }}
                                </div>
                            </div>
                          <div class="form-group ">
                            <div class="col-lg-10">
                                {{ form_row(form.photo,{'label':'Photo'} ) }}
                            </div>
                        </div>

                            <div class="act-in">
                                <div class=" col-lg-10">
                                    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
                                </div>
                            </div>
                        {{ form_end(form) }}
                    </div>

