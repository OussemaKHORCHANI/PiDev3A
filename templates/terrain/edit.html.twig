{% extends 'base2.html.twig' %}

{% block title %}Edit Terrain{% endblock %}
{% block head %}
<section id="main-content">
    <section class="wrapper">
        <!--overview start-->
        <div class="row">
            <div class="col-lg-12">
                {% endblock %}
                {% block showterrain %}

    <h1>Modifier Terrain</h1>

    {{ include('terrain/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('terrain_index') }}" class="btn btn-primary">Retour</a>

    {{ include('terrain/_delete_form.html.twig') }}
            </div>
        </div>
    </section>
</section>
{% endblock %}
