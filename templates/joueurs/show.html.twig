{% extends 'base2.html.twig' %}

{% block title %}Joueurs{% endblock %}

{% block head %}
<section id="main-content">
    <section class="wrapper">
        <!--overview start-->
        <div class="row">
            <div class="col-lg-12">
                {% endblock %}
                {% block showterrain %}
    <h1>Joueur: {{ joueur.nom }} {{ joueur.prenom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ joueur.nom }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ joueur.prenom }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ joueur.age }}</td>
            </tr>
            <tr>
                <th>NomClub</th>
                <td>{{ joueur.idClub.nomClub }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ joueur.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('joueurs_index') }}"><button class="btn btn-primary">{{ button_label|default('Back to list!') }}</button></a>

    <a href="{{ path('joueurs_edit', {'id': joueur.id}) }}"> <button class="btn btn-success">Edit</button></a>

    {{ include('joueurs/_delete_form.html.twig') }}
            </div></div></section></section>
{% endblock %}
