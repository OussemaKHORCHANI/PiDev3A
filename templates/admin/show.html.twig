{% extends 'base2.html.twig' %}

{% block title %}Admin{% endblock %}

{% block head %}
<section id="main-content">
    <section class="wrapper">
        <!--overview start-->
        <div class="row">
            <div class="col-lg-12">
                {% endblock %}
                {% block showterrain %}
    <h1>Admin</h1>

    <table class="table">
        <tbody>

        <tr>
            <th>Nom</th>
            <td>{{ admin.nom }}</td>
        </tr>
        <tr>
            <th>Prenom</th>
            <td>{{ admin.prenom }}</td>
        </tr>
        <tr>
            <th>Username</th>
            <td>{{ admin.username }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ admin.email }}</td>
        </tr>

        </tbody>
    </table>

    <a href="{{ path('admin_index') }}" class="btn btn-primary">Retour</a>

    <a href="{{ path('admin_edit', {'id': admin.id}) }}" class="btn btn-success">Modifier</a>

    {{ include('admin/_delete_form.html.twig') }}
            </div></div></section></section>
{% endblock   %}
