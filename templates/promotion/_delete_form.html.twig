<form method="post" action="{{ path('promotion_delete', {'idPromo': promotion.idPromo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ promotion.idPromo) }}">
    <button class="btn">Delete</button>
</form>
