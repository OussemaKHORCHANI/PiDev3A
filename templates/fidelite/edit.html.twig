{% extends 'base.html.twig' %}

{% block title %}Edit Fidelite{% endblock %}

{% block body %}
    <h1>Edit Fidelite</h1>

    {{ include('fidelite/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('fidelite_index') }}">back to list</a>

    {{ include('fidelite/_delete_form.html.twig') }}
{% endblock %}
